[pytest]
# Demo of the table-fixture scaffolding (fixtures.py) against pure duckdb.
# Unlike a real .test consumer (which `duck-test configure`s python_files off), this
# demo's bodies ARE python — so keep pytest's default test_*.py collection on.
addopts = --import-mode=importlib
